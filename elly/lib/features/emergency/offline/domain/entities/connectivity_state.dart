/// connectivity_state.dart
///
/// Comprehensive connectivity state enum representing device network & hardware connection states.

library;

enum ConnectivityState {
  online,
  offline,
  reconnecting,
  airplaneMode,
  noPermission,
  limited,
}
